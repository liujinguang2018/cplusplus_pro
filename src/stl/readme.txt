======不改变操作对象的算法=====
1. Linear Search   ///查找单个元素
    find
    find_if
    adjacent_find
    find_first_of
2. Subsequence matching    ////子序列匹配
    search
    find_end     ///search_end
    search_n
3. counting elements
    count
    count_if
4. for_each
5. ranges comparing
    equal
    mismatch
    lexicographical_compare
5. min & max
    min
    max
    min_element
    max_element
    

